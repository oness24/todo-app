# Django Core Settings
DJANGO_SECRET_KEY='your_actual_secret_key_here_if_not_using_default'
DJANGO_DEBUG='True' # Set to 'False' in production
DJANGO_ALLOWED_HOSTS='localhost,127.0.0.1' # Example for dev, MUST be changed for prod

# CORS Settings for Production (when DEBUG=False)
# DJANGO_CORS_ALLOWED_ORIGINS='https://yourfrontenddomain.com,https://www.yourfrontenddomain.com'
DJANGO_CORS_ALLOWED_ORIGINS='' # e.g., http://localhost:8080 for dev if not using CORS_ALLOW_ALL_ORIGINS=True

# AWS S3 Static Files Storage (Optional - leave blank if not using S3)
AWS_ACCESS_KEY_ID='AKIAUOGIMFM5BFWEEI54'
AWS_SECRET_ACCESS_KEY='GHf8x+cL+AMu7i4VwJXD1At6DOaoy08XCFDc91uX'
AWS_STORAGE_BUCKET_NAME='AWS_S3_CUSTOM_DOMAIN'
AWS_S3_REGION_NAME='South America (São Paulo)' # e.g., us-east-1
# AWS_S3_CUSTOM_DOMAIN='' # Optional: If you have a CDN or a different S3 domain format

# Sentry DSN (Optional - leave blank if not using Sentry)
SENTRY_DSN='' 